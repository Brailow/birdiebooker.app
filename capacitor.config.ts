import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'birdiebooker_site',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
